"use client";

/**
 * A small red asterisk used to mark required fields.
 * Matches the style from the Ask User node's "Question *" label.
 */
export function RequiredIndicator() {
  return <span className="text-destructive">*</span>;
}

