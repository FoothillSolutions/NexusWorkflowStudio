app_type: "frontend-only"
tech_stack:
  backend: null
  frontend: "react-nextjs"
directories:
  frontend: "src"
commands:
  start_backend: null
  start_frontend: "npm run dev"
  syntax_check: "npm run typecheck"
  lint: "npm run lint"
  test: null # no test runner detected
  type_check: "npm run typecheck"
  build: "npm run build"
  db_reset: null
ports:
  backend: null
  frontend: 3000
has_web_ui: true
env_files: []
